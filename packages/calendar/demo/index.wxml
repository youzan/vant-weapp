<demo-block title="平铺展示">
  <van-calendar
    title="日历"
    poppable="{{ false }}"
    show-confirm="{{ false }}"
    min-date="{{ 1326124800000 }}"
    max-date="{{ 1332172800000 }}"
    first-day-of-week="{{ firstDayOfWeek }}"
    class="tiled-calendar"
  />
</demo-block>
