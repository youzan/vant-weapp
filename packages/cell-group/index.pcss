@import "../common/_mixins";

.cell-group {
  position: relative;
  background-color: #fff;
  overflow: hidden;

  &::after {
    @mixin hairline;
    border-top-width: 1px;
    border-bottom-width: 1px;
  }
}

.cell-group--without-border::after {
  display: none;
}
