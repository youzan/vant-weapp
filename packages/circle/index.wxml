<view class="van-circle">
  <canvas class="van-circle__canvas" id="vanCircle" style="{{style}}" canvas-id="vanCircle"></canvas>
  <view wx:if="{{!text}}" class="van-circle__text">
    <slot></slot>
  </view>
  <cover-view wx:else class="van-circle__text">{{text}}</cover-view>
</view>
