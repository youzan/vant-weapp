<view class="custom-class van-sticky {{ containerClass }}" style="z-index: {{ zIndex }}; {{ containerStyle }}">
  <view class="van-sticky-wrap" style="{{ wrapStyle }}">
    <slot />
  </view>
</view>
