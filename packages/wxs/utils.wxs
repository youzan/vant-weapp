var bem = require('./bem.wxs').bem;
var memoize = require('./memoize.wxs').memoize;
var suffixPx = require('./suffixPx.wxs').suffixPx;

module.exports = {
  bem: memoize(bem),
  memoize: memoize,
  suffixPx: suffixPx,
};
